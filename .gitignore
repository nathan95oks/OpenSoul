# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Temporales de herramientas (auditoría LSB, entornos Python, node)
/scratch/
/venv/
node_modules/
/docs/node_modules/
/docs/mermaid_images/

# Configuración local con endpoints AWS (leída por run.ps1 / run.sh).
# .env.example sí se versiona como plantilla.
.env
.env.*
!.env.example

# Salidas locales de tareas puntuales (imágenes generadas, listados de
# pendientes, recorridos de depuración) — no son parte del código versionado.
/generadas/
/imagenes_pendientes.tsv
/docs/recorrido-lsb_to_text_audio.md
/docs/recorrido-paso-a-paso-lsb_to_text_audio.md

# Ruta al Flutter SDK local, generada por `flutter build`.
android/local.properties
