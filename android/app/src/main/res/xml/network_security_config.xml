<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuración de seguridad de red para OpenSoul.
  Permite cleartext (HTTP) SOLO para localhost (requerido por model_viewer_plus).
  Todo el tráfico externo (AWS S3, Lambda) sigue usando HTTPS.
-->
<network-security-config>
    <!-- Permite HTTP solo para el servidor local interno de model_viewer_plus -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

    <!-- El resto del tráfico (S3, API Gateway) usa HTTPS por defecto -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
